//permite trabajar sobre un array y saber si esta ahi un elemento

//crear en una variable let llamada numbers un arreglo
let numbers = [1, 3, 4, 6, 7, 8];

//buscar un numero con el metodo includes en el array que creamos para que devuelva true
console.log(numbers.includes(4));

//buscar un texto con el metodo includes en el array que creamos para que devuelva true
let list = ['razo', 'Frank', 'Lili', 'Erick',];

console.log(list.includes('frank'));